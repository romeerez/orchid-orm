import { IsQuery, Query } from '../../query';
import { RelationConfigBase } from '../../relations';
export declare const _chain: (fromQuery: IsQuery, toQuery: IsQuery, rel: RelationConfigBase) => Query;
