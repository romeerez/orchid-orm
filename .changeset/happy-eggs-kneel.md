---
'orchid-orm': minor
'pqb': minor
---

Infer `belongsTo` requiredness from local column nullability when `required` is omitted, so non-nullable foreign keys behave as required relations by default (#420)
